'use strict'
let fs = require('fs')

class FileTool {
    constructor(config) {
        this.config = config
    }
    getRootDir() { // 获取根目录

    }
    getFileDir() { // 获取指定文件路径

    }
    getFilesTotal() { // 获取指定文件的数量

    }
}

let tool = new FileTool();